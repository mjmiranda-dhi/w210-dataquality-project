#Literally, the backend.

def preprocess_data(filename):
   #generates viz and stats
   pre_data = {
       'viz': '/static/img/tree_graph.html', #IMG_6254.png',    #for now viz is a word. it should be html or something
       #'viz': '/static/img/radial_degree_test.png', #IMG_6254.png',    #for now viz is a word. it should be html or something
       'stats': 'pre stats' 
   } 
   return pre_data
